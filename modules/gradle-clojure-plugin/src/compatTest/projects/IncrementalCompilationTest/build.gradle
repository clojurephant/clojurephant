plugins {
  id "gradle-clojure.clojure" apply false
}

subprojects {
  apply plugin: 'gradle-clojure.clojure'

  repositories {
    mavenCentral()
    mavenLocal()
  }

  compileClojure {
    options.aotCompile = Boolean.getBoolean("aotCompile")
  }
}
