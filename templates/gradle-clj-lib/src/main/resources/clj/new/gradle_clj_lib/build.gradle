plugins {
  id 'gradle-clojure.clojure' version '@gradle-clojure.version@'
  id 'maven-publish'
}

group = '{{group}}'
version = '0.1.0-SNAPSHOT'

repositories {
  {{#maven-local}}
  mavenLocal()
  {{/maven-local}}
  mavenCentral()
  maven {
    name = 'clojars'
    url = 'https://repo.clojars.org'
  }
}

dependencies {
  implementation 'org.clojure:clojure:1.10.0-RC2'

  testImplementation 'junit:junit:4.12'

  devImplementation 'org.clojure:tools.namespace:0.3.0-alpha4'
}

publishing {
  publications {
    main(MavenPublication) {
      from components.java
    }
  }
  repositories {
    maven {
      name = 'clojars'
      url = 'https://repo.clojars.org'
      credentials {
        username = System.env['CLOJARS_USER']
        password = System.env['CLOJARS_PASSWORD']
      }
    }
  }
}
